# Python API and local env (frontend is Vite-only on Vercel)
backend/
requirements.txt
.python-version
.venv/
venv/

# ML checkpoints and training artifacts (served by backend, not the static app)
models/

# Source datasets at repo root (app ships curated copies under public/ when needed)
data/

# Backend-oriented docs
docs/prediction-backend-context.md
